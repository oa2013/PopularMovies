<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingStart="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingEnd="@dimen/activity_horizontal_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".MainActivity">

    <Button
        android:id="@+id/sort_button_popularity"
        android:layout_width="match_parent"
        android:layout_height="@dimen/button_width"
        android:padding="@dimen/button_padding"
        android:text="@string/sort_popularity"
        app:layout_constraintBottom_toTopOf="@+id/rv_posters"
        app:layout_constraintTop_toTopOf="@+id/sort_button_rating"/>

    <Button
        android:id="@+id/sort_button_rating"
        android:layout_width="match_parent"
        android:layout_height="@dimen/button_width"
        android:padding="@dimen/button_padding"
        android:text="@string/sort_rating"
        app:layout_constraintBottom_toTopOf="@+id/sort_button_popularity"
        app:layout_constraintTop_toTopOf="parent"/>

    <android.support.v7.widget.RecyclerView
        android:id="@+id/rv_posters"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="128dp"
        android:layout_marginBottom="@dimen/activity_horizontal_margin"
        android:scrollbars="vertical"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="1.0">

    </android.support.v7.widget.RecyclerView>

</android.support.constraint.ConstraintLayout>